对，你这个直觉有一半是对的：**reference pose 确实应该在 GateB 里被重点采样，因为它很可能靠近 policy 熟悉的区域；但它不能成为 GT Basin 的搜索空间本身。** 这里最关键的是把“reference 是什么”和“GT Basin 是什么”分开。

先说 N2M。更准确地讲，我们现在验证的是 N2M 的核心经验前提，而不是完整复现 N2M：学习型 manipulation policy 对 initial pose 有明显偏好，而单纯满足几何 reachability 并不足以保证成功。N2M 本身也是通过 rollout 学“preferable initial pose”，官方实现的数据标签就是从 manipulation rollout 的成功结果获得的。 你的 GateA 更进一步地说明了两件事：第一，reachable 和 policy 熟悉的 reference 之间确实存在巨大 gap；第二，更重要的是，**reachable region 内部本身不是均匀的**，而存在 \(0\rightarrow1\) 这种非常大的 \(S_\pi(p)\) 变化。这第二点才真正把 SkillBasin 推出来。

我们先严格定义一下。对于固定 scene \(s\) 和 frozen manipulation policy \(\pi\)，真正想测的是一个函数：

\[
S_\pi(p\mid s)
=
P(\text{task success}\mid p,s,\pi).
\]

然后 GT Basin 可以定义成：

\[
B_{\pi,s}^{\tau}
=
\left\{
p\in\mathcal P_{\rm feasible}:
S_\pi(p\mid s)\ge\tau
\right\}.
\]

注意这里的定义域应该是一个**物理上有资格被尝试的 pose 空间** \(\mathcal P_{\rm feasible}\)，而不是 reference 附近。因为我们要回答的问题是：“在这个 scene 里，机器人究竟站在哪里，这个 frozen policy 能成功？”这实际上是在测一张完整的 success landscape。

这就是为什么我之前说“从 reachable poses 里找”。这里的 reachable 不是因为我们相信 reachability baseline，而恰恰相反，它只是一个**搜索空间过滤器**。最外层所有 SE(2) pose 中有大量位置机器人根本停不了、机械臂根本够不到目标或者一定碰撞。如果把这些也全部拿来做 GT，你得到的大量标签只是毫无信息量的

\[
S_\pi(p)=0.
\]

这些 0 告诉你的只是“机械臂够不到”，而不是“这个 frozen policy 不喜欢这个站位”。SkillBasin真正想研究的是：**在已经满足基本几何可行性的条件下，为什么有的 pose 成功、有的失败。**

所以可以把逻辑想成：

\[
\mathcal P_{\rm all}
\xrightarrow{\text{geometry / reachability}}
\mathcal P_{\rm feasible}
\xrightarrow{\text{policy rollout}}
S_\pi(p)
\xrightarrow{\text{threshold}}
B_\pi .
\]

第一步只是排除“根本不能做”的点；第二步才是 SkillBasin 的核心。

而 reference pose 属于完全不同的一层。它更像一个训练分布先验：

\[
p_{\rm ref}\sim q_{\rm demo}(p).
\]

也就是说，reference 告诉我们：“这个 policy 在训练/示范时大概习惯从这里开始。”所以你的直觉完全合理——**reference 周围确实应该有较高的采样密度。** 但是 reference 不能等同于 basin，更不能用它来限定 basin 的范围。因为这样会产生一个很严重的循环论证：我们先假定“好 pose 应该靠近 reference”，然后只在 reference 附近采样，最后得到结论“好 pose 果然靠近 reference”。这就不是发现 basin，而是在验证自己的先验。

而你现在 GateA 已经给出了一个非常漂亮的反例：`Stove scene2 seed1` 中，

\[
S_\pi(p_{\rm ref})=0,
\]

但是 reachable pool 中却有

\[
\max_{p\in P_{\rm reach}}S_\pi(p)=0.667.
\]

这一个结果几乎就足够解释为什么**不能从 reference region 定义 GT**。如果 GateB 只在 reference 周围采样，你很可能得到一大片失败点，然后错误地认为“这个 policy 在这个 scene 根本不会做这个任务”；实际上几十厘米之外可能存在一个完全不同的高成功率区域。

更进一步，真实的 basin 很可能根本不是一个“围绕 reference 的圆”。它可能是偏移的、很窄的，甚至有多个 disconnected islands：

\[
B_\pi
=
B_\pi^{(1)}
\cup
B_\pi^{(2)}.
\]

例如 policy 可能既可以从物体正前方成功，也可以从右前方成功，但中间某些角度因为视觉遮挡、腕部姿态或 action distribution shift 反而失败。如果我们只围着 reference 做局部 perturbation，就永远看不到第二个 island。那样最终训练出来的世界模型也只是在“预测 reference 附近成功不成功”，而不是 SkillBasin 所声称的“发现 frozen policy 的 success domain”。

所以我现在会把 GateB 的采样策略改成一个更准确的表述：**GT Basin 的定义域来自 geometry-feasible / reachable space，但采样分布不应该纯均匀。reference 应该作为一个重要 anchor。** 也就是既要有全局探索，又要有 reference 周围的局部高分辨率采样。粗略可以理解成：

\[
\underbrace{\text{global feasible coverage}}_{\text{防止漏掉未知 basin}}
+
\underbrace{\text{reference-centered dense sampling}}_{\text{测熟悉区域}}
+
\underbrace{\text{adaptive refinement}}_{\text{细化 basin 边界}}.
\]

因此，你的直觉不是“错”，而是 reference 应该放在**采样策略**这一层，而不是放在**GT 定义域**这一层。

还有一个非常重要的地方：我现在其实不太想继续叫 GateB 的空间严格意义上的 `P_reach`，因为这容易让人误会我们是在接受 N2M 的 reachable region 作为真理。论文里最好把它改成更中性的：

\[
\boxed{\mathcal P_{\rm geo}}
\]

或者

\[
\boxed{\mathcal P_{\rm feasible}}
\]

表示“经过最基本 collision / navigation / kinematic feasibility 筛选之后的候选 base pose domain”。然后在这个 domain 上测：

\[
S_\pi(p).
\]

reference 只是里面一个特殊的 anchor：

\[
p_{\rm ref}\in\mathcal P_{\rm feasible}.
\]

这样论文故事会非常干净：**N2M 证明/我们 GateA 再次验证了 geometry feasibility 不等于 policy preference；SkillBasin 不再直接学习‘一个 preferable pose’，而是首先测量整个 feasible domain 上的 policy success landscape，然后再让世界模型去近似这张 landscape。**

所以 GateB 真正要得到的东西其实不是“GT 最佳 pose”，而是一张类似这样的地图：

\[
p=(x,y,\theta)
\quad\longrightarrow\quad
S_\pi(p)\in[0,1].
\]

reference 可能落在高值区，也可能落在低值区；最终 oracle 也不是“reference”，而是

\[
p_{\rm oracle}
=
\arg\max_{p\in\mathcal P_{\rm feasible}}
S_\pi(p).
\]

这就和你 GateA 现在看到的结果完全接上了。

一句话概括就是：**reference 告诉我们“policy 从哪里学来的”；GT Basin 要告诉我们“policy 实际在哪里能赢”。前者是 prior，后者是 ground truth。reachable/feasible region 则只是规定“哪些地方值得问这个问题”。** 这三者不要混在一起。

而且我认为这个区分会让 SkillBasin 比 N2M 的故事更有力量：N2M 的目标主要是预测 preferable initial pose distribution；SkillBasin 如果成立，目标则是显式恢复

\[
\mathcal P_{\rm feasible}
\rightarrow S_\pi(p)
\]

这整个函数，再从中选择站位。N2M 官方也明确把问题描述成从 rollout 学 policy-specific initial-pose preference，而不是仅靠几何 reachability。

下一步设计 GateB 时，我会据此把你原先的“48–64 poses”重新拆成**全局 feasible poses + reference 邻域 poses + 后续边界加密 poses**，这样 GT basin 才既不会被 reference bias，又不会因为均匀采样太稀而看不清 basin 的形状。
