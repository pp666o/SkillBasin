# SkillBasin：让冻结的机器人技能在想象中先排练一遍，找到它能赢的起点

> **文档版本**：v2.0 / 2026-09-04（通俗重写版，为主阅读文档；v1.5 旧版保留为技术细节档案，实验参数与协议细节以旧版为准）
> **目标会议**：CVPR 2027（截稿预计 2026-11-13，官方 CFP 发布后须核对）；退路 RSS / CoRL 2027
> **暂定英文题目**：*Learning Where a Frozen Skill Becomes Controllable*
> **当前状态**：所有效果均为待验证假设，第一关 Gate A 须在 09-14 前出结论

---

## 1. 我们要解决什么问题

移动操作机器人到了目标附近，最后要做一个看似不起眼的决定：**底盘停在哪里，再开始用机械臂干活。**

实际经验是：停的位置差几十厘米、转个角度，一个本来会做这个任务的操作策略（VLA）就做不成了。因为它训练时只见过有限的视角和相对位姿——偏出去之后，目标被遮挡、够不着、接近方向不对、接触后没有控制裕量，各种失败都来了。所以"停哪儿"是移动操作的主要失败源之一。

这个问题已经有一条工作线在做，三篇代表作各给了一种回答，也各有一个盲区：

- **Mobi-π："站得让画面眼熟。"** 找一个站位，让策略看到的画面尽量接近训练分布。盲区：**看着眼熟不等于做得成功**——画面相似预见不了执行起来的碰撞、够不着、接触打滑这些闭环失败。
- **N2M："真刀真枪试出来。"** 让策略从不同站位真实执行很多次，记下成败，训练一个"场景 → 好站位"预测器。盲区：**每换一个新策略、新场景都要重新真实试错**——贵、慢，本质是记忆过去的成败，不是预测新情况。
- **MoMa-Kitchen："学一张通用的可操作站位地图。"** 大规模基准 + affordance 地图，学"站哪里够得着、干得了"。盲区：**这张地图和具体策略无关**——同一个位置，策略甲能成、策略乙就失败，通用地图回答不了"你手里这个策略行不行"。

## 2. 我们的主张

打个比方：问"球场上哪里是好投篮点"，必须先问**是谁投**——同一个位置，不同球员命中率完全不同。上面三篇给出的要么是"与投手无关的地图"（Mobi-π、MoMa-Kitchen），要么靠真投几百次来统计（N2M）。

我们的主张一句话：

> **不让策略真实执行一次，就画出专属于它的那张成功率地图。**

这张地图有个学名：这个冻结策略的 **basin（吸引域）**——从这些起点出发，闭环执行会被"吸"进成功。学术出身是经典控制里的 sequential composition：每个技能是一个漏斗，从漏斗口进去的状态都会收敛到目标；我们做的就是在 VLA 时代测量这个漏斗的形状——只不过漏斗形状不再靠 Lyapunov 分析推导，而是靠让技能在世界模型的想象里排练来测量。

用一张 2×2 表说清我们站在哪个格子（这也是论文的定位图）：

| | 需要真实执行 | 零执行 |
|---|---|---|
| **不建模动力学** | N2M（每个新策略/场景重新试错） | Mobi-π、MoMa-Kitchen 式地图（预见不了闭环失败） |
| **建模动力学** | 模拟器（评估真值，部署时没有） | **SkillBasin（独占象限）** |

**唯一主贡献**：把"最后一公里站位"重新定义为冻结技能的 basin 发现问题——策略专属、零执行。世界模型、候选搜索、判别器都只是实现这个命题的仪器部件，不单独包装成贡献。

## 3. 难题在哪

**难题一：不执行，怎么知道会不会成功？** 要判断"从这个站位开始行不行"，本来只有一个办法——真去执行。不执行就得预测整个闭环：机器人站过去会看到什么 → 策略看到这画面会做什么 → 这个动作会引起世界怎样的变化 → 策略接着又会做什么……一环扣一环，任何一环歪了结论就不可信。

**难题二：训练数据里根本没有这种完整过程。** 我们手里只有两堆互不相干的数据：一堆是底盘到处移动的视频（机械臂收着），一堆是底盘固定、机械臂操作的视频。**从来没有"先移动过去、然后开始操作"的配对数据**——采这种数据正是大家想避免的昂贵环节。所以模型必须在测试时，把分开学到的两种动力学（相机移动怎么改变视野、机械臂动作怎么改变物体）**首次组合**成它从没见过的完整过程。这是本提案最大的科学赌注，也是最大的卖点："零配对示范"（zero paired mobile-manipulation demonstrations）。

严格的措辞（论文里必须这样写）：我们没有目标机器人上"底盘 + 机械臂同时动、带动作标注"的配对轨迹；底盘单独移动的数据、固定基座的操作数据、通用视频预训练都允许，且完整披露。

## 4. 方法：一台仪器的三步

整个方法是对同一个对象（冻结技能的 basin）的三步测量。论文方法节就按这三步分节，不按模型组件分节——这是消除"A+B+C 模块堆叠"观感的关键。

### 4.1 提名（propose）

在目标周围撒几十个候选站位（均匀/扇形/碰撞约束采样，第一版不训练候选网络）。先用零成本的几何过滤（碰撞、可达、可见）把几十个压到 8–12 个，世界模型只精评短名单——几何过滤器同时就是对照基线，一举两得。

### 4.2 排练（rehearse）——仪器的核心

对每个候选站位：

1. 世界模型先想象"如果我停在那儿，会看到什么画面"（这个能力来自底盘移动数据）；
2. 把想象的画面交给**真实的冻结 VLA**，它输出它真实会做的动作块（chunk，约 1–2 秒的动作序列）；
3. 世界模型根据这个动作块预测"世界会变成什么样"（这个能力来自固定基座操作数据）；
4. 新画面再给 VLA，再拿动作，再预测……闭环转 2–3 个动作块。

**关键分工：世界模型从不猜策略会做什么**——每一步都真的去问 VLA；它只负责"做了会怎样"。而物理不认策略：给定画面和动作预测后果，跟动作是谁出的无关。所以策略换了，排练结果自动跟着变——这正是"策略专属"的来源，也是 Mobi-π/N2M/MoMa-Kitchen 都给不出的性质。

**转几圈就够：推到"决定性事件"为止，约想象 3–8 秒，不推到任务结束。** 判断站位好坏不需要看到任务完成，只需要看到分水岭：夹爪闭合、物体离开台面；末端碰到把手、抽屉产生正确方向的初始位移；柜门把手被抓稳、开始朝正确方向转。直觉是：站位的影响在开头几秒就兑现了——够不着、视角不对、接近方向错，都会在第一次接触前后暴露。只推几秒还有个重要副作用：避开视频模型长时程漂移的老毛病。

### 4.3 裁决（verdict）

排练结果不由世界模型自己打分，由独立的裁判读数：

- **任务判别器**看想象画面序列，打任务进展分（把手位移了吗、物体抬起来了吗）；
- 加上碰撞风险、到达代价；
- 每个站位随机排练 M 次（约 5 次），**取偏低的分位数而不是平均**——排练结果不稳定的站位自动被罚分，想象不可靠的地方宁可不选。

几十个站位各得一分，排序，热图出来，选最稳的那个，真实导航过去，让同一个冻结 VLA 真实执行。

**裁判的纪律（防作弊）**：判别器只准看画面里的任务状态变化，不准接收站位信息；训练数据只准用固定基座操作数据或任务定义——否则它偷偷变成"成功率预测器"，"零执行"主张就被它偷渡掉了。

## 5. 世界模型从哪来

**不自己从零训。** 全领域没有一家从零训骨干（DECOWAM、OSCAR、MobileWAM、ABot 全部继承预训练视频模型），我们也一样：

- **底座**：Cosmos-Predict2.5-2B（预训练视频世界模型）。互联网规模预训练已经把"相机移动时场景怎么变、物体大概怎么动"这些通用先验灌进去了，这部分白嫖——跨场景的泛化能力主要由它提供；
- **我们训的部分**：动作条件后训练，教它听懂两种动作。用底盘数据教自运动分支（给定相机轨迹，画面怎么变），用操作数据教交互分支（给定机械臂几何轨迹，物体和接触怎么变）。**两个条件分支参数独立、各自由一半数据监督，测试时首次同时激活**——这个"因子化条件设计"是被分离数据逼出来的架构选择，也是论文的条件性架构贡献之一（细节见 §9）；
- **算力有先例**：OSCAR 就是在同一个 Cosmos-2B 上、单卡 GH200 完成动作条件后训练并证明有效。这是"给预训练模型加条件接口"级别的投入，不是"训大模型"级别。

**注意一个容易误解的点：我们不需要"会导航推理"的世界模型。** 路线规划、避障交给现成的标准导航器；候选站位定了，底盘轨迹和相机位姿序列就完全确定了（几何换算，不用学习）。世界模型只回答一个视觉问题："相机沿这条已知轨迹走，会看到什么画面"——这是已知相机轨迹的视频生成，比"通用导航世界模型"容易得多，而且范围只限最后一公里、目标已可见。

**操作数据的推荐来源：就用被评估的 VLA 自己录。** 让这个 VLA 在固定基座、各种偏移视角下大量执行并录制（自然包含成功与失败）。这样世界模型字面意义上"见过这个 VLA 的动作如何改变世界"——只是从没见过换站位后的组合，而那正是要验证的假设。这不违反"零配对"主张（禁止的只是底盘 + 机械臂的配对轨迹）。**负例必须够多**（抓偏、滑落、撞击、遮挡后失控）：只喂成功视频，视频模型会学会无视动作、直接生成"看起来成功"的画面。

**一个已知的硬骨头：腕部相机。** 多数强 VLA 同时吃第三人称和腕部相机，而腕部视角随机械臂运动剧烈变化，是视频生成最难的通道。首版二选一：(a) 选只用第三人称视角的冻结策略；(b) 用机械臂正运动学推导腕部相机位姿（外参完全确定），把问题退化为已知相机轨迹的生成。此决定影响策略选型，须在 Gate B 前锁定。

## 6. 立意防御：为什么"先想"而不是"直接干"

这是最根本的质疑（内部已提出，审稿人也必问）："人不会在脑子里排练一万遍，走近了总能抓到，想象还有误差——先想再做的立意是不是就有问题？"五点回答，前两点写进论文 Introduction：

1. **人的类比方向反了。** 人可以"直接干"，是因为人的闭环纠错极强、basin 巨大，从哪儿开始都行；冻结 VLA 的 basin 又小又怪——这不是假设，是 Mobi-π/N2M/MoMa-Kitchen 整条工作线存在的理由。**basin 小的执行者才需要挑入口。** 而且人在失败昂贵、难撤销的场合（侧方停车、台球选位、搬重物站位）同样先选起点再动手——思考量与决策的不可逆性成正比。我们只把想象花在唯一难撤销的决策（从哪开始）上；执行本身完全是真实闭环，不被想象替代。
2. **"走近了总能抓到"是可检验命题，我们第一步就检验它。** 它就是 nearest-pose 基线 + Gate A（见 §7）。headroom 小 → 前提不成立，项目在训练任何模型之前止损；headroom 大 → 质疑被第一手数据回答，数据本身就是动机图。
3. **试错重试正式入组当基线。** "走到最近站位直接干，失败就挪一挪再试"，给它与我们相同的总时间预算（我们的想象耗时按真实时钟计入自己成本），正面比较。失败不可重置的任务（碰倒会翻的容器、推错会卡死的抽屉）单独分层——重试路线在这类任务上结构性受损。任务集因此必须包含 1–2 个不可重置任务。
4. **想象有误差——所以我们只要求它排序对，不要求预测准。** 序数要求比预测准低一大截；只推 3–8 秒；多次排练取低分位罚不稳定；还有动作一致性诊断（见 §7 Gate C）在任何结果之前先检查"想象是否扭曲了策略的行为"。误差不是被否认的，是被整个协议围着管理的。
5. **"想一万遍"发生在离线。** 排练是慢教师；蒸馏出来的快速 basin 预测头是快学生（一次前向直接出整张图，见 §9）。部署时机器人不站在原地深思——和"新手停车想很久、老手一把进"同构。

## 7. 怎么验证、怎么止损：四道关卡

项目按四道关卡推进，每道关卡都有明确的"过/停"标准——立意不是赌出来的，是第一个实验测出来的。

**Gate A（09-14 前）：问题存在吗？** 在训练任何世界模型之前，用模拟器测：默认/最近站位的成功率 vs 全场最优站位的成功率，差距（pose headroom）有多大。只保留 headroom 明显、且好站位不能被"距离最近/够得着"简单解释的任务。**headroom 小 → 停止方向，零成本退出。**

**Gate B（10-05 前）：评估的尺子。** 用模拟器真实执行，建立每个任务、每个场景、每个候选站位、每个策略的真值成功率地图（GT basin）。规模下限：6–8 个任务 × 每任务 20+ 场景 × 48–64 站位 × 2–3 个冻结策略 × ≥5 种子 ≈ 10 万次 rollout。**这份数据本身就是论文承诺开源的 benchmark**（命名候选：BasinBench / DockBasin），是 CVPR 认可度最高的贡献形态之一。

**Gate C（10-24 前）：仪器准吗？** 让世界模型给同一批站位打分，和真值地图比：排序相关性（Spearman）、Top-k 命中、选点后悔值。有一个前置诊断必须最先报告——**动作一致性**：同一状态下，VLA 看真实画面和看想象画面输出的动作差多少。如果差得多，说明想象已经扭曲了策略行为，整个评估回路被污染，后面的数字都不可信。端到端总指标是 **ROH（恢复了多少 oracle headroom）**：从默认站位到最优站位的差距，我们的选点追回了几成——首轮目标追回一半以上。

**Gate D（真机，CVPR 周期外）：** 选点 → 真实导航 → 真实观测 → 同一冻结 VLA 真实执行。世界模型只用来选起点，绝不把生成视频里的动作直接发给机器人。真机验证留给 RSS/CoRL 版或 rebuttal。

**10-25 Go/No-Go**：Gate C 排序正相关、且两个架构消融至少一条显著 → 继续 CVPR；否则转投 RSS 2027（1 月底截稿），多出的时间补真机。

## 8. 实验设计要点

**泛化设置（从易到难）**：训练/测试严格分离的组合检验 → 换物体实例和布局 → **换冻结策略**（世界模型不动，换一个 VLA，验证 basin 图跟着变——这是"策略专属"主张的直接检验）→ 换任务。主论文主打前三个，第四个进补充材料。

**对照组**（每类挑代表）：
- 几何类：最近可达、最大可见性、可操作度、随机可行站位——回答"是不是几何就够了"；
- 试错类：**最近站位 + 等预算重试**——回答"是不是直接干就够了"（§6 第 3 点）；
- 学习类：Mobi-π 式画面相似、N2M 式 rollout 偏好、直接成功率分类器——在 held-out 场景和 held-out 策略上比，故事落点是"靠想象泛化，不靠记忆"；
- 世界模型类：V-JEPA 2-AC 潜空间模型、更小的生成模型——回答"是不是必须这么大的模型"；
- 上界：模拟器 oracle。

**最关键的消融**（全部围绕主命题）：
1. 去掉 VLA 动作条件 / 换成平均或随机动作——证明评分必须针对被部署的那个策略；
2. 开环一次性生成 vs 闭环逐块排练——证明闭环必要；
3. 因子化双分支 vs 单体条件化——架构主张的直接判据；
4. 均值评分 vs 低分位评分；不同排练时长（决定性事件够不够）；
5. 换策略后 basin 是否跟着变。

## 9. 两个条件性的架构贡献（消融成立才写进贡献）

CVPR 审稿人会问"架构上有什么新的"（我们复用 Cosmos 骨干）。两个回答，都是被问题逼出来的设计、不是外挂模块，且各挂一个消融，不成立就撤：

1. **因子化动作条件化**（§5）：自运动与交互两个条件分支参数独立、各自由一半数据监督、测试时首次合体。同期的 DECOWAM 用过相似手法（须引用），但它服务于配对数据下的参数高效控制，我们服务于**零配对分离数据的组合**——主张收缩为"因子化使能零配对组合"。
2. **摊销 basin 头 + 策略指纹**（第二阶段）：用排练标注的（场景、站位、策略、成功率）数据，蒸馏一个"一次前向输出整张 SE(2) basin 热图"的快头；为了对没见过的策略泛化，不用策略 ID 做条件，而用"该策略在一组固定探针观测上的动作响应"做行为指纹。这就是"策略条件的地面 affordance"的字面实现，也是部署实时性的正式回答。

## 10. 与最近工作的边界（一段话版）

四类近邻，各一句划界：**站位选择线**（Mobi-π/N2M/MoMa-Kitchen/FloAff-Kitchen）——见 §1 三个盲区，我们占 2×2 表的空象限；**世界模型评估策略线**（OSCAR/World Action Planner/GeniWorld）——它们评估动作或固定基座下的策略鲁棒性，我们评估**初始条件**、处理移动相机、用分离数据（OSCAR 与我们同骨干，区分必须靠协议与 policy-swap 实验做实）；**统一世界-动作模型线**（ABot/MobileWAM/DECOWAM）——它们学完整全身控制、需要配对数据，我们刻意不学控制、只做测量；**"冻结策略进想象回路"家族**（PiJEPA/CheckVLA/ω-EVA）——它们改进控制或单步校验，我们做多步闭环排练以评估初始条件，且消费者是不可修改的像素接口 VLA（这正是我们必须用像素级世界模型的原因：一批潜空间快速路线靠"重训一个吃 latent 的动作头"逃离像素慢，我们的消费者冻结不可改，像素接口是问题定义的必然推论）。

经典出身一段：漏斗与吸引域组合（Burridge–Rizzi–Koditschek 1999）、options 的 initiation set（Sutton–Precup–Singh 1999）、技能链（Konidaris–Barto 2009）、SayCan 的价值函数 affordance——论文定位为"**用学习的世界模型补上经典 sequential composition 缺失的吸引域估计**"，这是全文最大的叙事升级。注意：**不宣称"层次化世界模型"**——层次在决策结构里（先选站位、后执行技能），不在世界模型内部，宣称了反而招不利对比。

## 11. 时间线与人员（CVPR 2027 倒排）

| 时间 | 里程碑 |
|---|---|
| 09-01 – 09-14 | 锁定冻结 VLA（2–3 个）、任务（6–8 个，含 1–2 个失败不可重置）、仿真家族；跑通 **Gate A**；搭 GT 生成管线 |
| 09-15 – 10-05 | **Gate B**：GT basin 批量生成（即 benchmark 本体）；Cosmos-2B 动作条件后训练，因子化与单体两版并行 |
| 10-06 – 10-24 | **Gate C**：完整分离组合 + 因子化消融；若排序为正，启动摊销头与换策略实验 |
| **10-25** | **Go/No-Go**：过 → 冲 CVPR；不过 → 转 RSS 2027，补真机 |
| 10-25 – 11-13 | 写作、主图、supplementary video（teaser 三联：候选站位 → 想象排练 → basin 热图；换策略前后热图对比动画） |

CVPR 周期内强制裁剪：纯仿真、单一仿真家族、真机完全移出（留 rebuttal / RSS 版）。人员：SkillBasin 学生一作总负责；研二做世界模型后训练基建（此后转方向 D）；研零 #1 做 GT 管线与基线复现；研零 #2 做指标与可视化资产。

## 12. 主要风险与止损（精选）

- **冻结 VLA 本身太弱**（所有站位都失败）→ Gate A 用 oracle 上界先筛任务；
- **几何就能解决**（可达性基线接近 oracle）→ 优先方向敏感、接触丰富、遮挡明显的任务；仍不行则停止或收缩结论；
- **分离数据组合失败**（画面逼真但排序无关）→ 这正是因子化架构针对的风险；诊断靠"真实中间帧 vs 想象中间帧"的分步定位；组合彻底失败则立即停止扩大模型；
- **想象扭曲策略行为**（动作一致性差）→ Gate C 前置诊断暴露；同时这份数据是后续"策略等效世界模型"方向（07 号调研文档方向 D）的立项侦察——失真严重反而证明了那个方向的必要性；
- **腕部相机生成不可靠** → §5 的二选一预案，Gate B 前锁定。

## 13. 一句话总结

> 别人在学"哪里站得好"这张通用地图；我们在造一台仪器，测量"你手里这一个策略在哪里能赢"——测量方式是让它在想象里先打一遍，而这台仪器准不准，由一份 10 万次真实 rollout 的公开基准来裁决。

---

## 附：版本关系

- **v2.0（本文档）**：通俗重写版，主阅读文档。叙事结构：问题与三个参照物 → 主张与 2×2 → 两个难题 → 提名/排练/裁决 → 世界模型来源 → 立意防御 → 四道关卡 → 实验 → 架构点 → 边界 → 时间线 → 风险。
- **v1.5（`06_SkillBasin_世界模型发现冻结VLA可控域_提案_v1.0.md`）**：技术细节档案。数学定义、完整消融清单、全部审稿问答（Q1–Q9）、数据隔离审计、逐条引用与划界、投稿分析全文保留在该版，写论文时以它为准。
